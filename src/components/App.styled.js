import styled from 'styled-components';

export const Container = styled.div`
  max-width: 1440px;
  margin-top: 16px;
  margin-inline: auto;
  display: grid;
  grid-template-columns: 1fr;
  grid-gap: 16px;
  padding-bottom: 24px;
`;
