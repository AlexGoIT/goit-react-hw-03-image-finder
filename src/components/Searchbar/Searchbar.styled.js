import styled from 'styled-components';

export const SearchForm = styled.form`
  max-width: 500px;
  display: flex;
  align-items: center;
  background-color: #fff;
  border-radius: 3px;
  overflow: hidden;
`;
